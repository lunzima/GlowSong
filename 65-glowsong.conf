<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "urn:fontconfig:fonts.dtd">
<!--
  GlowSong fontconfig rules.

  To the extent possible under law, The GlowSong Authors have waived all
  copyright and related or neighbouring rights to this file. Licensed under
  CC0 1.0 Universal; the full text is in LICENSE-CC0. Copy it, change it, ship
  it, no attribution needed.

  SPDX-License-Identifier: CC0-1.0
-->
<fontconfig>

  <!-- Monospace classification.

       fontconfig derives spacing from advance widths, so a half-width ASCII
       beside full-width ideographs comes out FC_DUAL (90), not FC_MONO (100).
       Pango accepts 90; Qt and TDE require 100 and leave the face out of their
       fixed-width lists. Neither post.isFixedPitch nor PANOSE is consulted, so
       this cannot be fixed inside the font.

       The same derivation errs at the other end. Ext A carries Extension A
       and nothing else, every glyph in it is full width, and so every advance
       in it is the same. That reads as FC_MONO (100) and puts a fallback face
       nobody selects into the same fixed-width lists, as a choice that draws
       nothing at all for ordinary text. Ext A is not a monospace typeface.

       At scan time, because those lists are built from the cache. Run
       `fc-cache -f` after installing this file. -->

  <match target="scan">
    <test name="family"><string>GlowSong Mono</string></test>
    <edit name="spacing" mode="assign"><const>mono</const></edit>
  </match>
  <match target="scan">
    <test name="family"><string>GlowSong GB Mono</string></test>
    <edit name="spacing" mode="assign"><const>mono</const></edit>
  </match>
  <match target="scan">
    <test name="family"><string>GlowSong ExtA</string></test>
    <edit name="spacing" mode="assign"><const>proportional</const></edit>
  </match>

  <!-- Rendering.

       The embedded strikes are at 12, 13, 14, 15 and 16 px and there are no
       others. FreeType rounds a fractional request to the nearest whole pixel
       size, halves upward, and takes a strike only if one sits at that size,
       so the requests that reach a bitmap are 11.5 up to but not including
       16.5: 11.5 rounds to 12 and lands, 11.4 rounds to 11 and does not, 16.4
       rounds to 16 and lands, 16.5 rounds to 17 and does not.

       Both bounds matter: a rule that states only one of them gives the sizes
       outside the interval, which have no bitmap to draw, the settings that
       are meant for the sizes inside it.

       Inside the interval the bitmaps draw, and antialiasing would blur what
       they exist to keep sharp. Outside it the outlines take over with their
       own instructions, and hintfull is what runs them: hintslight would
       substitute FreeType's autohinter and discard them.

       The sizes below the interval are asked for. Kicker derives its menu
       fonts downwards into them: k_new_mnu.cpp:424 sets the tab bar to 4/5 of
       the menu font, which is 8pt and so 10.67px at 96 DPI when that font is
       10pt, and itemview.cpp:158-162 derives the menu's title and description
       sizes the same way. With no lower bound those controls were drawing an
       unhinted, unantialiased outline. -->

  <match target="font">
    <test name="family" compare="contains"><string>GlowSong</string></test>
    <test name="pixelsize" compare="less"><double>11.5</double></test>
    <edit name="embeddedbitmap" mode="assign"><bool>false</bool></edit>
    <edit name="antialias" mode="assign"><bool>true</bool></edit>
    <edit name="hinting" mode="assign"><bool>true</bool></edit>
    <edit name="hintstyle" mode="assign"><const>hintfull</const></edit>
  </match>
  <match target="font">
    <test name="family" compare="contains"><string>GlowSong</string></test>
    <test name="pixelsize" compare="more_eq"><double>11.5</double></test>
    <test name="pixelsize" compare="less"><double>16.5</double></test>
    <edit name="embeddedbitmap" mode="assign"><bool>true</bool></edit>
    <edit name="antialias" mode="assign"><bool>false</bool></edit>
    <edit name="hinting" mode="assign"><bool>false</bool></edit>
  </match>
  <match target="font">
    <test name="family" compare="contains"><string>GlowSong</string></test>
    <test name="pixelsize" compare="more_eq"><double>16.5</double></test>
    <edit name="embeddedbitmap" mode="assign"><bool>false</bool></edit>
    <edit name="antialias" mode="assign"><bool>true</bool></edit>
    <edit name="hinting" mode="assign"><bool>true</bool></edit>
    <edit name="hintstyle" mode="assign"><const>hintfull</const></edit>
  </match>

  <!-- Ext A is a fallback, not a family anyone selects. Appended weakly so a
       character outside CP936 resolves instead of becoming a box. -->

  <match target="pattern">
    <test name="family" compare="contains"><string>GlowSong</string></test>
    <edit name="family" mode="append" binding="weak">
      <string>GlowSong ExtA</string>
    </edit>
  </match>

  <!-- Generic classification.

       Each face says which generic it belongs to, so that a picker filtering
       by generic finds it. Miss one and fontconfig treats it as unclassified,
       then presses it into service as some other generic.

       There is deliberately no <prefer> here. Installing a font should not
       decide what the desktop's default serif or monospace is; that is the
       user's setting to make. -->

  <alias>
    <family>GlowSong</family>
    <default><family>serif</family></default>
  </alias>
  <alias>
    <family>GlowSong GB</family>
    <default><family>serif</family></default>
  </alias>
  <alias>
    <family>GlowSong ExtA</family>
    <default><family>serif</family></default>
  </alias>
  <alias>
    <family>GlowSong Mono</family>
    <default><family>monospace</family></default>
  </alias>
  <alias>
    <family>GlowSong GB Mono</family>
    <default><family>monospace</family></default>
  </alias>

  <!-- Old family names. Documents and pages that hard-code them are common
       enough that without these the font may as well not be installed. -->

  <match target="pattern">
    <test name="family"><string>SimSun</string></test>
    <edit name="family" mode="prepend" binding="strong">
      <string>GlowSong</string>
    </edit>
  </match>
  <match target="pattern">
    <test name="family"><string>宋体</string></test>
    <edit name="family" mode="prepend" binding="strong">
      <string>GlowSong</string>
    </edit>
  </match>
  <match target="pattern">
    <test name="family"><string>NSimSun</string></test>
    <edit name="family" mode="prepend" binding="strong">
      <string>GlowSong Mono</string>
    </edit>
  </match>
  <match target="pattern">
    <test name="family"><string>新宋体</string></test>
    <edit name="family" mode="prepend" binding="strong">
      <string>GlowSong Mono</string>
    </edit>
  </match>

</fontconfig>
